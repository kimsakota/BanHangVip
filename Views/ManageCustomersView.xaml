<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BanHangVip.ViewModels"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             x:Class="BanHangVip.Views.ManageCustomersView"
             Title="Quản lý Khách hàng"
             BackgroundColor="#f8fafc"
             Shell.NavBarIsVisible="False"
             ios:Page.UseSafeArea="True">

    <Grid RowDefinitions="Auto, *">
        <!-- Header -->
        <Grid Grid.Row="0" Padding="15,15" BackgroundColor="White" ColumnDefinitions="Auto, *, Auto">

            <!-- Nút Back dùng Vector Icon (Path) thay vì Text để tránh lỗi font -->
            <Border StrokeShape="RoundRectangle 20" 
                    BackgroundColor="Transparent" 
                    StrokeThickness="0" 
                    HeightRequest="40" 
                    WidthRequest="40" 
                    Padding="0">
                <Border.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding GoBackCommand}"/>
                </Border.GestureRecognizers>
                <!-- Hình mũi tên vẽ bằng Path -->
                <Path Data="M20,11H7.83l5.59-5.59L12,4l-8,8l8,8l1.41-1.41L7.83,13H20V11z" 
                      Fill="#1e293b" 
                      Aspect="Uniform"
                      HeightRequest="24"
                      WidthRequest="24"
                      HorizontalOptions="Center" 
                      VerticalOptions="Center"/>
            </Border>

            <Label Grid.Column="1" 
                   Text="Khách hàng" 
                   TextColor="#1e293b" 
                   FontAttributes="Bold" 
                   FontSize="20" 
                   HorizontalOptions="Center"
                   VerticalOptions="Center"/>

            <!-- Nút Thêm -->
            <Button Grid.Column="2"
                    Text="+" 
                    Command="{Binding AddCustomerCommand}"
                    BackgroundColor="#0369a1" 
                    TextColor="White" 
                    WidthRequest="40" 
                    HeightRequest="40" 
                    CornerRadius="20"
                    Padding="0"
                    FontSize="24"/>
        </Grid>

        <!-- Danh sách -->
        <CollectionView Grid.Row="1" ItemsSource="{Binding Customers}" Margin="15">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Border StrokeShape="RoundRectangle 15" BackgroundColor="White" Padding="15" StrokeThickness="0">
                        <Border.Shadow>
                            <Shadow Brush="#cbd5e1" Offset="1,2" Radius="3" Opacity="0.1"/>
                        </Border.Shadow>
                        <Grid ColumnDefinitions="Auto, *">
                            <Border StrokeShape="RoundRectangle 20" BackgroundColor="#f0f9ff" WidthRequest="40" HeightRequest="40" StrokeThickness="0" Margin="0,0,15,0">
                                <Label Text="👤" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Border>
                            <Label Grid.Column="1" Text="{Binding .}" TextColor="#334155" FontAttributes="Bold" FontSize="16" VerticalOptions="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>