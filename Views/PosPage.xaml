<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BanHangVip.ViewModels"
             xmlns:model="clr-namespace:BanHangVip.Models"
             x:Class="BanHangVip.Views.PosPage"
             Title="Bán Hàng"
             BackgroundColor="#F5F5F5"
             Shell.NavBarIsVisible="False">

    <Grid>
        <!-- Layout Chính -->
        <Grid RowDefinitions="Auto, *, Auto">

            <!-- 1. Header: Chọn Khách -->
            <Grid Grid.Row="0" BackgroundColor="White" Padding="15,10" ColumnDefinitions="*, Auto">
                <Border Stroke="#0288D1" StrokeThickness="1" StrokeShape="RoundRectangle 10" Padding="10,0" HeightRequest="50">
                    <Grid ColumnDefinitions="30, *">
                        <Label Text="👤" VerticalOptions="Center"/>
                        <Picker Grid.Column="1" 
                                Title="Chọn Khách Hàng" 
                                ItemsSource="{Binding Customers}" 
                                SelectedItem="{Binding SelectedCustomer}"
                                FontAttributes="Bold"
                                TextColor="#333"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- 2. Danh sách Sản Phẩm (Grid) -->
            <CollectionView Grid.Row="1" ItemsSource="{Binding MenuItems}" Margin="10">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="{OnIdiom Phone=2, Tablet=3}" VerticalItemSpacing="10" HorizontalItemSpacing="10"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:SeafoodItem">
                        <Border StrokeShape="RoundRectangle 15" BackgroundColor="White" StrokeThickness="0" HeightRequest="130">
                            <Grid RowDefinitions="*, Auto">
                                <!-- Background màu nhẹ -->
                                <BoxView Color="{Binding ColorHex}" Opacity="0.15" Grid.RowSpan="2"/>

                                <!-- Icon/Ảnh -->
                                <Label Text="🦀" FontSize="40" HorizontalOptions="Center" VerticalOptions="Center"/>

                                <!-- Tên -->
                                <Label Grid.Row="1" Text="{Binding Name}" 
                                       FontSize="16" FontAttributes="Bold" 
                                       HorizontalOptions="Center" Padding="5" 
                                       TextColor="#333"/>
                            </Grid>
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PosViewModel}}, Path=SelectProductCommand}" CommandParameter="{Binding .}"/>
                            </Border.GestureRecognizers>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- 3. Bottom Widget: Giỏ hàng & Nút Lưu -->
            <!-- Chỉ hiện khi có món trong giỏ (HasItems) -->
            <Grid Grid.Row="2" IsVisible="{Binding HasItems}">
                <Border StrokeShape="RoundRectangle 20,20,0,0" 
                        BackgroundColor="White" 
                        StrokeThickness="0"
                        Padding="0">
                    <Border.Shadow>
                        <Shadow Brush="Black" Offset="0,-5" Radius="10" Opacity="0.1"/>
                    </Border.Shadow>

                    <VerticalStackLayout>
                        <!-- Phần mở rộng: Chi tiết các món (Hiện khi bấm vào thanh tổng) -->
                        <CollectionView ItemsSource="{Binding CurrentCart}" 
                                        HeightRequest="150" 
                                        IsVisible="{Binding IsCartExpanded}"
                                        Margin="15">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="model:OrderItem">
                                    <Grid ColumnDefinitions="*, Auto, Auto" Padding="0,5">
                                        <Label Text="{Binding Item.Name}" VerticalOptions="Center"/>
                                        <Label Grid.Column="1" Text="{Binding DisplayWeight}" FontAttributes="Bold" TextColor="#0288D1" Margin="10,0"/>
                                        <Button Grid.Column="2" Text="🗑️" BackgroundColor="Transparent" TextColor="Red" 
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PosViewModel}}, Path=RemoveItemCommand}" 
                                                CommandParameter="{Binding .}"/>
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <!-- Thanh Tổng Hợp (Luôn hiện) -->
                        <Grid ColumnDefinitions="Auto, *, Auto" Padding="15" BackgroundColor="#0288D1">
                            <!-- Tap vùng này để mở rộng xem chi tiết -->
                            <HorizontalStackLayout Spacing="10" VerticalOptions="Center">
                                <HorizontalStackLayout.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding ToggleCartExpandCommand}"/>
                                </HorizontalStackLayout.GestureRecognizers>

                                <Border BackgroundColor="White" StrokeShape="RoundRectangle 15" Padding="10,5">
                                    <Label Text="{Binding TotalCartItems}" TextColor="#0288D1" FontAttributes="Bold"/>
                                </Border>
                                <VerticalStackLayout Spacing="0">
                                    <Label Text="{Binding TotalCartWeight, StringFormat='Tổng: {0:F2} kg'}" TextColor="White" FontAttributes="Bold" FontSize="16"/>
                                    <!-- Dùng DataTrigger thay vì Converter để tránh lỗi thiếu file -->
                                    <Label Text="▲ Xem chi tiết" TextColor="#E1F5FE" FontSize="11">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding IsCartExpanded}" Value="True">
                                                <Setter Property="Text" Value="▼ Thu gọn" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                </VerticalStackLayout>
                            </HorizontalStackLayout>

                            <!-- Nút Lưu Đơn -->
                            <Button Grid.Column="2" 
                                    Text="LƯU ĐƠN NGAY" 
                                    Command="{Binding SaveOrderCommand}"
                                    BackgroundColor="#FF9800" 
                                    TextColor="White"
                                    FontAttributes="Bold"
                                    CornerRadius="20"
                                    HeightRequest="45"/>
                        </Grid>
                    </VerticalStackLayout>
                </Border>
            </Grid>
        </Grid>

        <!-- 4. POPUP Nhập Kg (Overlay toàn màn hình) -->
        <Grid BackgroundColor="#CC000000" IsVisible="{Binding IsPopupVisible}" ZIndex="99">
            <Border BackgroundColor="White" VerticalOptions="End" StrokeShape="RoundRectangle 25,25,0,0" Padding="20">
                <VerticalStackLayout Spacing="15">
                    <!-- Header Popup -->
                    <Grid ColumnDefinitions="*, Auto">
                        <Label Text="{Binding SelectedProduct.Name}" FontSize="22" FontAttributes="Bold" TextColor="#333"/>
                        <Button Grid.Column="1" Text="✕" BackgroundColor="Transparent" TextColor="#333" Command="{Binding ClosePopupCommand}"/>
                    </Grid>

                    <!-- Màn hình hiển thị số -->
                    <Border Stroke="#0288D1" StrokeThickness="2" BackgroundColor="#E1F5FE" Padding="15" HeightRequest="80">
                        <Label Text="{Binding WeightInput}" FontSize="45" HorizontalOptions="Center" VerticalOptions="Center" TextColor="#0277BD" FontAttributes="Bold"/>
                    </Border>

                    <!-- Gợi ý nhanh -->
                    <Grid ColumnDefinitions="*,*,*">
                        <Button Text="0.5 kg" Command="{Binding QuickAddWeightCommand}" CommandParameter="0.5" BackgroundColor="#F5F5F5" TextColor="#333"/>
                        <Button Grid.Column="1" Text="1.0 kg" Command="{Binding QuickAddWeightCommand}" CommandParameter="1" BackgroundColor="#F5F5F5" TextColor="#333" Margin="10,0"/>
                        <Button Grid.Column="2" Text="2.0 kg" Command="{Binding QuickAddWeightCommand}" CommandParameter="2" BackgroundColor="#F5F5F5" TextColor="#333"/>
                    </Grid>

                    <!-- Bàn phím số -->
                    <Grid ColumnDefinitions="*,*,*" RowDefinitions="65,65,65,65" RowSpacing="10" ColumnSpacing="10">
                        <Button Grid.Row="0" Grid.Column="0" Text="7" Command="{Binding InputNumCommand}" CommandParameter="7" Style="{StaticResource NumpadButtonStyle}"/>
                        <Button Grid.Row="0" Grid.Column="1" Text="8" Command="{Binding InputNumCommand}" CommandParameter="8" Style="{StaticResource NumpadButtonStyle}"/>
                        <Button Grid.Row="0" Grid.Column="2" Text="9" Command="{Binding InputNumCommand}" CommandParameter="9" Style="{StaticResource NumpadButtonStyle}"/>

                        <Button Grid.Row="1" Grid.Column="0" Text="4" Command="{Binding InputNumCommand}" CommandParameter="4" Style="{StaticResource NumpadButtonStyle}"/>
                        <Button Grid.Row="1" Grid.Column="1" Text="5" Command="{Binding InputNumCommand}" CommandParameter="5" Style="{StaticResource NumpadButtonStyle}"/>
                        <Button Grid.Row="1" Grid.Column="2" Text="6" Command="{Binding InputNumCommand}" CommandParameter="6" Style="{StaticResource NumpadButtonStyle}"/>

                        <Button Grid.Row="2" Grid.Column="0" Text="1" Command="{Binding InputNumCommand}" CommandParameter="1" Style="{StaticResource NumpadButtonStyle}"/>
                        <Button Grid.Row="2" Grid.Column="1" Text="2" Command="{Binding InputNumCommand}" CommandParameter="2" Style="{StaticResource NumpadButtonStyle}"/>
                        <Button Grid.Row="2" Grid.Column="2" Text="3" Command="{Binding InputNumCommand}" CommandParameter="3" Style="{StaticResource NumpadButtonStyle}"/>

                        <Button Grid.Row="3" Grid.Column="0" Text="." Command="{Binding InputNumCommand}" CommandParameter="." Style="{StaticResource NumpadButtonStyle}" FontAttributes="Bold"/>
                        <Button Grid.Row="3" Grid.Column="1" Text="0" Command="{Binding InputNumCommand}" CommandParameter="0" Style="{StaticResource NumpadButtonStyle}"/>
                        <Button Grid.Row="3" Grid.Column="2" Text="⌫" Command="{Binding InputNumCommand}" CommandParameter="DEL" BackgroundColor="#FFEBEE" TextColor="Red" FontSize="24"/>
                    </Grid>

                    <Button Text="THÊM MÓN" BackgroundColor="#0288D1" TextColor="White" FontAttributes="Bold" HeightRequest="60" CornerRadius="15" FontSize="18" Command="{Binding AddToCartCommand}"/>
                </VerticalStackLayout>
            </Border>
        </Grid>

        <Grid.Resources>
            <Style x:Key="NumpadButtonStyle" TargetType="Button">
                <Setter Property="BackgroundColor" Value="White"/>
                <Setter Property="TextColor" Value="#333"/>
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="BorderColor" Value="#EEE"/>
                <Setter Property="BorderWidth" Value="1"/>
                <Setter Property="Shadow">
                    <Shadow Brush="Black" Offset="0,2" Radius="2" Opacity="0.1"/>
                </Setter>
            </Style>
        </Grid.Resources>
    </Grid>
</ContentPage>